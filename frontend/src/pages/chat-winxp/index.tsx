/**
 * Experimental Windows-XP-themed chat shell. The whole module is opt-in
 * via the `WinXpToggle` button and can be removed by deleting this folder
 * and the two-line wiring in `App.tsx`.
 */

export { WinXpExperiment } from './WinXpExperiment'
export { WinXpToggle, useWinXpEnabled } from './WinXpToggle'
